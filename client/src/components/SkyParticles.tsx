import React from 'react';
import styled from 'styled-components';

// Este componente agora é apenas um espaço reservado, pois o particles.js 
// é inicializado diretamente no HTML para maior compatibilidade

const ParticlesContainer = styled.div`
  /* O elemento #particles-js já existe no index.html */
`;

const SkyParticles: React.FC = () => {
  // Não precisamos fazer nada aqui, pois o particles.js é inicializado no HTML
  return null;
};

export default SkyParticles;